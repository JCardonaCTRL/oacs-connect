import React, { useEffect, useRef } from 'react';
import { View, Text, } from 'react-native';

export default IdeasSettingsScreen = () => {
    return(
        <View>
            <Text>
                settings
            </Text>
        </View>
    );
}