diff --git a/node_modules/react-navigation-tabs/lib/module/utils/withDimensions.js b/node_modules/react-navigation-tabs/lib/module/utils/withDimensions.js
index 50bb8e7..e663668 100644
--- a/node_modules/react-navigation-tabs/lib/module/utils/withDimensions.js
+++ b/node_modules/react-navigation-tabs/lib/module/utils/withDimensions.js
@@ -14,6 +14,8 @@ export default function withDimensions(WrappedComponent) {
     constructor(props) {
       super(props);
 
+      _defineProperty(this, "dimensionChangeListener", void 0);
+
       _defineProperty(this, "handleOrientationChange", ({
         window
       }) => {
@@ -50,11 +52,11 @@ export default function withDimensions(WrappedComponent) {
     }
 
     componentDidMount() {
-      Dimensions.addEventListener('change', this.handleOrientationChange);
+      this.dimensionChangeListener = Dimensions.addEventListener('change', this.handleOrientationChange);
     }
 
     componentWillUnmount() {
-      Dimensions.removeEventListener('change', this.handleOrientationChange);
+      this.dimensionChangeListener.remove();
     }
 
     render() {
